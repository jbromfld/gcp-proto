# GCP Deployment Configuration
#
# USAGE:
#   1. Copy to project root: cp gcp-configs/env.template .env.gcp
#   2. Edit with your values: vim .env.gcp
#   3. Run setup: ./setup-gcp.sh
#
# This file is for GCP deployment only.
# For local development, use env.local.template instead.

# ============================================
# GCP PROJECT SETTINGS (REQUIRED)
# ============================================
# Your GCP project ID (e.g., my-rag-project-12345)
GCP_PROJECT_ID=your-project-id

# GCP region (e.g., us-central1, us-east1, europe-west1)
GCP_REGION=us-central1

# ============================================
# ELASTICSEARCH SETTINGS
# ============================================

# Option 1: Self-hosted on GCE (Recommended - FREE or ~$30/mo)
# This is the cost-effective option using Google's free tier or low-cost VMs
USE_GCE_ELASTICSEARCH=true
ELASTICSEARCH_MACHINE_TYPE=e2-micro  # FREE tier! Or e2-medium ($25/mo) for production
ELASTICSEARCH_DISK_SIZE_GB=30        # FREE tier! Or 50+ for production
ELASTICSEARCH_USE_PREEMPTIBLE=false  # Set to true for dev to save 60%

# Option 2: Elastic Cloud (Managed, but $95+/mo)
# Uncomment these if you prefer Elastic Cloud over GCE
# USE_GCE_ELASTICSEARCH=false
# ELASTICSEARCH_URL=https://your-deployment.es.us-central1.gcp.cloud.es.io:9243
# ELASTICSEARCH_PASSWORD=your-elastic-password
# ELASTICSEARCH_USERNAME=elastic

# ============================================
# DEPLOYMENT SETTINGS
# ============================================
# Use Terraform for infrastructure (recommended for production)
USE_TERRAFORM=true

# URLs to scrape for knowledge base (comma-separated)
SCRAPE_URLS=https://docs.python.org/3/tutorial/,https://fastapi.tiangolo.com/

# Allow unauthenticated access (set to false for production with auth)
ALLOW_UNAUTHENTICATED=true

# ============================================
# VERTEX AI SETTINGS
# ============================================
VERTEX_LLM_MODEL=gemini-1.5-flash
VERTEX_EMBEDDING_MODEL=text-embedding-004

